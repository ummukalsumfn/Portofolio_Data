**Business Intelligence Analyst Virtual Internship**

Menentukan Primary Key

Menentukan Relationship dari Ke-4 Tabel

Membuat Tabel Master
Query
SELECT
  o.Date AS order_date,
  pc.CategoryName AS category_name,
  p.ProdName AS product_name,
  p.Price AS product_price,
  o.Quantity AS order_qty,
  (o.Quantity * p.Price) AS total_sales,
  c.CustomerEmail AS cust_email,
  c.CustomerCity AS cust_city
FROM
  `soy-storm-354407.dataset.Orders` AS o
LEFT JOIN
  `soy-storm-354407.dataset.Customers` AS c ON o.CustomerID = c.CustomerID
LEFT JOIN
  `soy-storm-354407.dataset.Products` AS p ON o.ProdNumber = p.ProdNumber
LEFT JOIN
  `soy-storm-354407.dataset.ProductCategory` AS pc ON p.Category = pc.CategoryID
ORDER BY
  order_date ASC


Membuat Visualiasi Data Menggunakan Looker Studio

Recommendations
